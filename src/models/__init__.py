# Models are split by topic:
#   .config     — Config
#   .notice     — Alert, CentauriApiResponse          (sdcp/notice)
#   .status     — MachineStatus, StatusMessage, …     (sdcp/status, sdcp/response)
#   .attributes — AttributesMessage, …               (sdcp/attributes)
#   .discord    — DiscordEmbed, DiscordMessage, …
